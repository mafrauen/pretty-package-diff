{
  "name": "pretty-package-diff",
  "version": "0.2.0",
  "description": "Pretty prints dependency version change information from a yarn.lock file",
  "bin": "./cli.js",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Bitbucket <dev@bitbucket.org>",
  "license": "ISC",
  "dependencies": {
    "chalk": "^3.0.0",
    "semver": "^7.1.1"
  },
  "devDependencies": {
    "prettier": "^1.19.1"
  }
}
